# Copia este archivo a .env y actualiza la URL de la BBDD
# Ejemplo para PostgreSQL local:
# DATABASE_URL="postgresql://usuario:password@localhost:5432/mecanico_db"

DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Puerto para el servidor Express
PORT=8000

# Opcional: URL para la shadow database usada por Prisma Migrate.
# Ejemplo: si creas una base llamada 'mecanico_shadow' y un usuario con permisos:
# SHADOW_DATABASE_URL="postgresql://mecanico_user:TU_PASSWORD@localhost:5432/mecanico_shadow"
